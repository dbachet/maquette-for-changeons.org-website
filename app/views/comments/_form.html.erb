<%= form_for(@comment) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
	<div class="drop-menu">
		Envoyer en tant que &nbsp; <span class="plus">></span>
		<ul class="sub-menu">
			<li>
				<%= link_to "Utilisateur enregistré", "#" %>
			</li>
			<li>
				<%= link_to "Invité", comments_show_comment_guest_fields_path, :remote => true %>
			</li>
		</ul>
	</div>
  </div>

	
	
<% end %>

